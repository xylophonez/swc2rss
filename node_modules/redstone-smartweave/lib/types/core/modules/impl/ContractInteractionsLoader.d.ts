import { GQLEdgeInterface, InteractionsLoader } from '../../..';
import Arweave from 'arweave';
export declare class ContractInteractionsLoader implements InteractionsLoader {
    private readonly arweave;
    private readonly logger;
    private static readonly query;
    private static readonly _30seconds;
    constructor(arweave: Arweave);
    load(contractTxId: string, fromBlockHeight: number, toBlockHeight: number): Promise<GQLEdgeInterface[]>;
    private getNextPage;
}
//# sourceMappingURL=ContractInteractionsLoader.d.ts.map